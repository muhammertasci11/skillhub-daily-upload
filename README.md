# SkillHub Daily Upload

Automatically uploads 832 MCP skills to Gumroad (10 per day).

## Setup

1. Add `GUMROAD_ACCESS_TOKEN` to repository secrets
2. Workflow runs daily at 10:00 UTC
3. Progress tracked in `upload-progress.json`
